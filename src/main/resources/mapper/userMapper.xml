<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.joonggo.repository.UserMapper">
    <insert id="insert">
        insert into user(user_id, password, user_name, birth_year, birth_month, birth_day, address)
        values(#{userId}, #{password}, #{userName}, #{birthYear}, #{birthMonth}, #{birthDay}, #{address})
    </insert>
    <select id="selectUserById" parameterType="String" resultType="com.project.joonggo.domain.UserVO">
        SELECT * FROM user WHERE user_id = #{userId} AND is_del = 'N'
    </select>

</mapper>